'use strict'

exports.plugin = function hemera{{=data.name}} (options, next) {
  const hemera = this
  const topic = '{{=data.topic}}'

  hemera.add({
    topic,
    cmd: 'add'
  }, (req, cb) => {
    cb(null, req.a + req.b)
  })

  next()
}

exports.options = {}

exports.attributes = {
  pkg: require('./package.json'),
  dependencies: []
}
