'use strict'

const Hp = require('hemera-plugin')

const plugin = Hp(hemera{{=data.name}},  {
  hemera: '>=3',
  name: '{{=data.name}}',
  dependencies: ['hemera-joi'],
  options: {}
})

function hemera{{=data.name}} (hemera, opts, done) {
  const topic = '{{=data.topic}}'

  hemera.add({
    topic,
    cmd: 'add'
  }, (req) => req.a + req.b)

  done()
}

module.exports = plugin
